# 🚀 Quick Integration Guide
# دليل التكامل السريع

---

## ✅ اكتمل الدمج بنجاح!

تم دمج نظام **ASO (After-Sales Operations)** مع **TSH ERP Ecosystem** بنجاح.

---

## 📁 الملفات المضافة

### 1. في النظام الرئيسي

```
/app/models/after_sales.py          # نماذج قاعدة البيانات (8 models)
/app/models/__init__.py              # تم التحديث لتضمين ASO models
/app/routers/aso/__init__.py         # Router رئيسي لـ ASO
```

### 2. في التطبيق المحمول

```
/apps/prss/mobile-tech/lib/config/app_config.dart    # إعدادات الاتصال بالنظام المركزي
/apps/prss/mobile-tech/lib/main.dart                 # التطبيق الكامل مع الإشعارات
```

### 3. الوثائق

```
/apps/prss/ASO_INTEGRATION_GUIDE.md       # دليل التكامل المفصل
/apps/prss/INTEGRATION_COMPLETE.md        # ملخص الإكمال
/apps/prss/QUICK_INTEGRATION_GUIDE.md     # هذا الملف
```

---

## 🎯 الخطوات الثلاث للتشغيل

### الخطوة 1: تشغيل النظام الرئيسي

```bash
cd /Users/khaleelal-mulla/TSH_ERP_Ecosystem
source .venv/bin/activate
python -m uvicorn app.main:app --reload --port 8000
```

✅ النظام سيُنشئ جداول ASO تلقائياً عند التشغيل للمرة الأولى

### الخطوة 2: تشغيل التطبيق المحمول

```bash
cd /Users/khaleelal-mulla/TSH_ERP_Ecosystem/apps/prss/mobile-tech
flutter clean
flutter pub get
flutter run -d home --release
```

### الخطوة 3: تسجيل الدخول

```
Username: technician1
Password: tech123
```

---

## 🔗 الاتصال بين النظامين

### قاعدة البيانات
```
✅ نفس قاعدة البيانات: erp_db
✅ جداول ASO: aso_*
✅ جداول المستخدمين: users, roles, permissions (مشتركة)
```

### API
```
✅ Base URL: http://localhost:8000
✅ Auth: /api/auth/login
✅ ASO: /aso/*
```

### المصادقة
```
✅ نفس JWT tokens
✅ نفس SECRET_KEY
✅ نفس نظام RBAC
```

---

## 📱 ميزات التطبيق المحمول

1. ✅ **Dashboard**: نظرة عامة على المهام
2. ✅ **Notifications**: نظام إشعارات كامل مع badge
3. ✅ **Maintenance Jobs**: قائمة مهام الصيانة
4. ✅ **QR Scanner**: مسح أكواد المنتجات
5. ✅ **Arabic UI**: واجهة عربية 100%
6. ✅ **Demo Mode**: التجربة بدون اتصال

---

## 🎨 لقطات الشاشة

### الشاشة الرئيسية
- أيقونة الإشعارات مع عداد (3)
- بطاقة المستخدم
- إحصائيات المهام
- قائمة الإجراءات

### شاشة الإشعارات
- قائمة الإشعارات مع أيقونات ملونة
- تمييز غير المقروءة
- السحب للحذف
- زر "تحديد الكل كمقروء"

---

## 🔧 التخصيص

### تغيير URL النظام المركزي

في `lib/config/app_config.dart`:
```dart
static const String baseUrl = 'http://your-server-ip:8000';
```

### تفعيل/تعطيل Demo Mode

```dart
static const bool isDemoMode = false; // false = live API
```

---

## 📊 الإحصائيات المتاحة

```http
GET /aso/reports/dashboard
```

- عدد طلبات الإرجاع
- معدل الموافقة/الرفض
- متوسط وقت المعالجة
- تكلفة الصيانة
- توزيع أسباب الإرجاع

---

## 🚨 حل المشاكل الشائعة

### مشكلة: التطبيق لا يتصل بالـ API

**الحل:**
```dart
// تأكد من تشغيل النظام الرئيسي على المنفذ 8000
// تأكد من baseUrl صحيح في app_config.dart
// جرب Demo Mode أولاً
```

### مشكلة: خطأ في المصادقة

**الحل:**
```bash
# تأكد من وجود المستخدم في قاعدة البيانات
# تحقق من SECRET_KEY متطابقة
```

### مشكلة: الإشعارات لا تظهر

**الحل:**
```dart
// تأكد من userId صحيح
// تحقق من endpoint الإشعارات
```

---

## 📚 مزيد من المعلومات

- **الدليل الكامل**: `ASO_INTEGRATION_GUIDE.md`
- **تفاصيل التكامل**: `INTEGRATION_COMPLETE.md`
- **API Docs**: `http://localhost:8000/docs`

---

## ✅ جاهز للاستخدام!

النظام الآن متكامل بالكامل وجاهز للاستخدام! 🎉

**التالي**: 
- إضافة المزيد من المستخدمين
- تخصيص الصلاحيات
- إضافة بيانات تجريبية
- تطوير لوحة التحكم Web

---

**آخر تحديث**: October 24, 2024
