{
  "name": "tsh-erp-testing",
  "version": "1.0.0",
  "description": "E2E testing for TSH ERP System",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:ui": "playwright test --ui",
    "test:user-dropdown": "playwright test user-management-dropdown.spec.ts",
    "test:user-dropdown:headed": "playwright test user-management-dropdown.spec.ts --headed",
    "test:user-dropdown:debug": "playwright test user-management-dropdown.spec.ts --debug",
    "playwright:install": "playwright install",
    "playwright:install-deps": "playwright install-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.0.0",
    "chrome-devtools-mcp": "^0.6.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "dotenv": "^17.2.3"
  }
}
