# Blue upstream configuration
# Place in: /etc/nginx/upstreams/tsh_erp_blue.conf

upstream tsh_erp_upstream {
    server 127.0.0.1:8001 max_fails=3 fail_timeout=30s;

    # Connection pooling
    keepalive 32;
    keepalive_timeout 60s;
    keepalive_requests 100;
}
